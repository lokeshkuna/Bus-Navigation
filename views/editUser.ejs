<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit User</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Custom CSS -->
    <style>
      /* Custom styles based on the selected color scheme */
      body {
        background-color: #f9e2a4; /* Light Peach */
        color: #34495e; /* Dark Navy Blue */
        padding-top: 56px; /* Set padding-top to accommodate fixed navbar */
      }

      .navbar {
        background-color: #c0392b; /* Crimson */
      }

      .navbar-brand {
        color: #ffffff; /* White */
      }

      .navbar-dark .navbar-nav .nav-link {
        color: rgba(255, 255, 255, 0.5); /* Lighter White */
      }

      .navbar-dark .navbar-nav .nav-link:hover {
        color: rgba(255, 255, 255, 0.75); /* Even Lighter White on Hover */
      }

      .card {
        border: none;
        border-radius: 10px;
        box-shadow: 0px 0px 10px 0px rgba(0, 0, 0, 0.1);
      }

      .card-header {
        background-color: #f08080; /* Coral */
        color: #ffffff; /* White */
        border-radius: 10px 10px 0 0;
      }

      .btn-primary {
        background-color: #d3545b; /* Crimson */
        border: none;
      }

      .form-group {
        margin-bottom: 20px;
      }

      label {
        font-weight: bold;
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
      <div class="container">
        <a class="navbar-brand" href="#">Edit User</a>
      </div>
    </nav>
    <div class="container mt-5">
      <div class="row justify-content-center">
        <div class="col-md-6">
          <div class="card">
            <div class="card-header">
              <h5 class="text-center">Edit User</h5>
            </div>
            <div class="card-body">
              <!-- Form for editing user -->
              <div><%=user%></div>
              <form action="/update/user/<%= user[0]._id %>" method="POST">
                <div class="form-group">
                  <label for="email">Email</label>
                  <input
                    type="email"
                    class="form-control"
                    id="email"
                    name="email"
                    value="<%= user[0].email %>"
                    required
                  />
                </div>
                <div class="form-group">
                  <label for="password">Password</label>
                  <input
                    type="password"
                    class="form-control"
                    id="password"
                    name="password"
                    value="<%= user.password %>"
                    required
                  />
                </div>
                <button type="submit" class="btn btn-primary btn-block">
                  Update User
                </button>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
